function inquire(){
    location.href = './contactUs.html';
}

function init(){
    let send = document.getElementsByClassName('contents')[0];
    send.scrollIntoView({behavior: 'smooth', block: 'end'});
}